export interface SubtitleSettings {
  language: string
  format: string
  timestamps: boolean
  speakerLabels: boolean
  autoTranslate: boolean
  burnVideo: boolean
  punctuation: boolean
  wordLevel: boolean
}
